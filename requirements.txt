# Core
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
accelerate>=0.27.0
deepspeed>=0.12.0

# Vision & 3D
opencv-python>=4.8.0
pillow>=10.0.0
trimesh>=4.0.0
open3d>=0.18.0
pyglet<2  # For trimesh visualization

# Data
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=15.0.0  # For parquet files
datasets>=2.18.0  # HuggingFace datasets

# DA3 (Depth Anything 3)
# Install from: https://github.com/DepthAnything/Depth-Anything-V3
# cd depth_anything_3 && pip install -e .

# VLM related
qwen-vl-utils>=0.0.8
autoawq>=0.2.0  # For quantization (optional)

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.1
scikit-learn>=1.4.0
scipy>=1.12.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Optional: Semantic labeling
timm>=0.9.0  # For DINOv2
open-clip-torch>=2.24.0  # For CLIP

# Optional: Flash Attention (CUDA only)
# flash-attn>=2.5.0  # Install separately: pip install flash-attn --no-build-isolation

# Development
pytest>=8.0.0
black>=24.0.0
flake8>=7.0.0
